# TODO: remove this config and env vars in docker run command once we figure out how to make mesos read config files
#       under /etc/mesos-master and /etc/mesos-slave. Based on testing, mesos in container doesn't seem to pick up the
#       values from config files

# master configs
MASTER_PORT=5050
QUORUM=1
REGISTRY=in_memory

# slave configs
AGENT_PORT=5051
SWITCH_USER=0
CONTAINERS=docker,mesos
LOG_DIR=/var/log/mesos
ISOLATION=filesystem/linux,namespaces/pid,docker/runtime,cgroups/cpu,cgroups/mem,disk/du
IMAGE_PROVIDERS=appc,docker
IMAGE_PROVISIONER_BACKEND=copy
APPC_STORE_DIR=/tmp/mesos/images/appc
WORK_DIR=/var/lib/mesos
RESOURCES="cpus(peloton):0.5;cpus(*):3.5;mem(peloton):1024;disk(*):20000;mem(*):1024;ports(peloton):[31000-32000]"
MODULES=file:///etc/mesos-slave/modules
RESOURCE_ESTIMATOR=org_apache_mesos_FixedResourceEstimator
