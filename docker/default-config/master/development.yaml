logging:
  level: info

mesos:
  host_port: "mesos-master:5050"
  zk_path: "zk://zookeeper:2181/mesos"
  framework:
    capabilities: ["gpu_resources"]

metrics:
  prometheus:
    enable: true

storage:
  use_cassandra: true
