package resmgr

const (
	// maxPlacementQueueSize is the max size of the placement queue.
	maxPlacementQueueSize = 1000 * 1000
	// mesosTaskID is the mesos task id string to use in statemachine
	mesosTaskID = "mesos_task_id"
)
