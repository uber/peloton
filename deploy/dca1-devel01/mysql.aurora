include('../template/mysql-template.aurora')

PROFILES = [
  Profile(
    cluster = 'dca1-devel01',
    docker_image = 'docker-registry01-sjc1:5055/uber-usi/infra/peloton-mysql5.7.17:latest',
    pinned_hosts = 'compute840-dca1',
  )
]

jobs = [
  JOB_TEMPLATE.bind(profile = p) for p in PROFILES
]
