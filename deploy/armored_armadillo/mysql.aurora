include('../template/mysql-template.aurora')

PROFILES = [
  Profile(
    cluster = 'armored_armadillo',
    pinned_hosts = 'ms-0d1a494c',
    # TODO: remove this override when T745768 is resolved
    docker_image = 'docker.int.uberatc.com/uber-usi/infra/peloton-mysql5.7.17',
    # TODO: remove this override once AA agent is launched with 'host' attribute
    pinned_attribute = 'hostname',
  )
]

jobs = [
  JOB_TEMPLATE.bind(profile = p) for p in PROFILES
]
