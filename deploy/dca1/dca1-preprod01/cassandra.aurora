include('../../template/cassandra-template.aurora')

PROFILES = [
  Profile(
    cluster = 'dca1-preprod01',
    pinned_hosts = 'compute77-dca1',
  )
]

jobs = [
  JOB_TEMPLATE.bind(profile = p) for p in PROFILES
]

# Initial setup is required, check out https://code.uberinternal.com/w/runbooks/peloton/operations/#cassandra
